# Local dev default (relative path, same-origin as the Vite dev server)
VITE_API_BASE_URL=/api/v1

# Production example (absolute URL to the deployed backend, including /api/v1)
# VITE_API_BASE_URL=https://devlens-api-pas3.onrender.com/api/v1
